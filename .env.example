# Copy to .env for optional signed release builds (do not commit .env)

# Developer ID Application certificate name (optional, for notarized DMG)
# APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID)"

# Optional notarytool keychain profile
# APPLE_NOTARIZE_PROFILE="trivor-notarize"
